# Example Secrets Configuration
# 
# This is a template file showing how to configure secrets for ACE-Box deployment.
# 
# IMPORTANT: 
# - Copy this file to 'secrets.yml' and add your actual values
# - NEVER commit secrets.yml to git (it's in .gitignore)
# - For production, use Ansible Vault to encrypt this file

---
# GitHub Personal Access Token (optional)
# Used for authenticated git operations
# Required for: private repositories, avoiding rate limits
# Create at: https://github.com/settings/tokens
# Required scope: repo
# Format: ghp_ followed by 36 characters
github_personal_access_token: "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Add other secrets as needed for your deployment
# Example:
# dynatrace_api_token: "your_dt_token_here"
# oauth_client_secret: "your_oauth_secret_here"
